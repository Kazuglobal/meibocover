# 📐 名簿表紙デザインシステム 全体設計書

## 🏗️ システム構成

### **フェーズ1（最優先）- 表紙デザイン機能**
1. **表紙プレビュー画面**
   - 用紙選択（レザック各種、プレスコート等）
   - 題字入力（9種類の指定書体）
   - 校章・ロゴアップロード（背景自動削除）
   - 箔押しオプション（金・銀）
   - リアルタイムプレビュー
   - 途中保存機能

2. **過去デザイン参照**
   - 学校名検索
   - サムネイル一覧表示
   - 参考デザインの選択・適用

### **フェーズ2 - 原稿管理機能**
1. **画像管理**
   - 前見返し（校舎・航空写真）
   - 口絵8ページ分
   - ドラッグ&ドロップ対応

2. **テキスト管理**
   - 各種挨拶文入力
   - ファイルアップロード（Word、Excel、一太郎、PDF）
   - OCR機能（手書き原稿対応）

### **フェーズ3 - ワークフロー機能**
1. **アクセス管理**
   - 顧客用URL（編集可能）
   - 営業用URL（閲覧のみ）
   - 製作部用URL（ダウンロード可能）

2. **進捗管理**
   - ステータス表示
   - 更新通知
   - 校正承認ボタン

### **フェーズ4 - 出力機能**
1. **データ出力**
   - Illustrator用指示書（PDF）
   - 画像一括ダウンロード
   - テキストデータ（CSV）

## 🔖 要件定義

### 1. 機能要件
| ID | 要件 | 優先度 |
|----|------|--------|
| FR-1 | 表紙プレビューで用紙・題字・校章・箔押しをリアルタイムに合成表示できること | 高 |
| FR-2 | 題字は 9 種類の指定書体から選択できること | 高 |
| FR-3 | 校章・ロゴ画像アップロード時に背景を自動削除できること | 高 |
| FR-4 | 箔押し（金・銀）を選択し、プレビューに反映できること | 中 |
| FR-5 | デザインを途中保存し、後から再編集できること | 高 |
| FR-6 | 過去デザインを学校名で検索し、サムネイル一覧から選択できること | 中 |
| FR-7 | 原稿画像（前見返し・口絵）をドラッグ＆ドロップでアップロードできること | 中 |
| FR-8 | ファイル（Word、Excel、一太郎、PDF）をアップロードしサーバ側で管理できること | 中 |
| FR-9 | 手書き原稿をOCR処理しテキスト化できること | 低 |
| FR-10 | 顧客・営業・製作部の各ロールに応じた URL／権限でアクセス制御できること | 高 |
| FR-11 | 進捗ステータスを表示し、更新時に通知できること | 高 |
| FR-12 | 校正承認フローをワンクリックで完了できること | 高 |
| FR-13 | Illustrator 指示書（PDF）、画像、テキストを所定形式で出力できること | 高 |

### 2. 非機能要件
| ID | 要件 | 目標値 |
|----|------|--------|
| NFR-1 | レスポンシブ対応：主要デバイス（PC／タブレット／スマホ）で UI が崩れないこと | 必須 |
| NFR-2 | プレビュー更新遅延 | 300ms 以内 |
| NFR-3 | システム稼働率 | 99.5% 以上（月間） |
| NFR-4 | データセキュリティ：通信を HTTPS で暗号化し、ファイルは 3 重冗長で保管 | 必須 |
| NFR-5 | アクセスログ保存期間 | 1 年 |
| NFR-6 | UX：主要操作は 3 クリック以内で完結 | 推奨 |

### 3. 制約条件
- バックエンドは Node.js（LTS）を使用すること。
- DB はリレーショナル（PostgreSQL）を想定すること。
- ブラウザは最新の Chrome／Edge／Safari、iOS/Android の主要ブラウザに対応すること。
- 外部サービス利用：背景削除 API は remove.bg 互換 API、OCR は Google Vision API を想定。

### 4. 想定ユーザ
1. **顧客（学校職員・生徒会）**：表紙デザイン作成、原稿アップロード
2. **営業担当**：進捗確認、顧客サポート
3. **製作部**：データダウンロード、品質チェック

### 5. 受け入れ基準
- すべての高優先度（優先度「高」）機能要件を満たし、関連テストケースがパスしていること。
- 非機能要件 NFR-1 ～ NFR-4 を満たしていること。
- UI／UX のフィードバックを 2 回以上反映し、重大な不具合がないこと。

## 💡 技術仕様

**フロントエンド**
- React（レスポンシブ対応）
- 自動保存機能
- プログレッシブWebアプリ（PWA）対応

**バックエンド**
- データベース（顧客情報、デザイン履歴）
- 画像処理API（背景削除）
- ファイル変換機能

**セキュリティ**
- ユニークURL生成
- アクセス期限設定（オプション）

## 📱 画面イメージ

1. **顧客画面**：直感的な操作で表紙デザイン作成
2. **営業画面**：進捗確認ダッシュボード
3. **製作画面**：データ一括ダウンロード 

## ✅ TODOリスト

### フェーズ1（最優先）- 表紙デザイン機能
- [x] UIデザイン：表紙プレビュー画面のワイヤーフレーム作成
- [x] 用紙選択コンポーネント実装
- [x] 題字入力（フォント切替対応）コンポーネント実装
- [x] 校章・ロゴアップロード機能（ドラッグ＆ドロップ対応）
- [x] 背景自動削除API連携
- [x] 箔押しオプション（金・銀）UI実装
- [x] リアルタイムプレビュー機能実装
- [x] 途中保存機能（ドラフト保存）実装
- [x] 過去デザインDBスキーマ設計
- [ ] 学校名検索API実装
- [ ] サムネイル一覧表示UI実装
- [ ] 参考デザイン適用機能実装

### フェーズ2 - 原稿管理機能
- [ ] 画像ストレージ設計（S3 等）
- [ ] 前見返し・口絵画像アップロード UI（ドラッグ＆ドロップ）
- [ ] 挨拶文入力フォーム実装
- [ ] ファイルアップロード（Word／Excel／PDF）機能実装
- [ ] OCR API 連携（手書き原稿）

### フェーズ3 - ワークフロー機能
- [x] URL/ロール管理設計（顧客／営業／製作部）
- [x] 進捗ステータスモデル実装
- [ ] 更新通知（メール／Slack）機能実装
- [ ] 校正承認ボタン実装

### フェーズ4 - 出力機能
- [ ] Illustrator 用指示書 PDF 生成機能
- [ ] 画像一括ダウンロード（ZIP）機能
- [ ] テキストデータ CSV 出力機能

### 共通・インフラタスク
- [x] データベース ER 図作成
- [x] REST/GraphQL API 設計
- [x] フロントエンド環境構築（React + Vite + TypeScript）
- [x] バックエンド環境構築（Node.js／Express）
- [ ] CI/CD パイプライン設定
- [ ] PWA 対応設定
- [x] セキュリティ：ユニーク URL 生成と有効期限設定
- [x] ドキュメント整備（README, API 仕様） 